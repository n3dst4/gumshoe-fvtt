import focusableSelectors from "focusable-selectors";

// this is so helpful - a great example of when it's okay to have a micropackage
// https://github.com/KittyGiraudel/focusable-selectors
export const focusableSelector = focusableSelectors.join(",");
