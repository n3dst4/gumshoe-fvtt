import { CSSObject } from "@emotion/css";

export const absoluteCover: CSSObject = {
  position: "absolute",
  top: 0,
  left: 0,
  right: 0,
  bottom: 0,
};

export const flexRow: CSSObject = {
  display: "flex",
  flexDirection: "row",
  // position: "relative",
};
export const flexColumn: CSSObject = {
  display: "flex",
  flexDirection: "column",
  // position: "relative",
};
